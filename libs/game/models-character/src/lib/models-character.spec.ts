import {gamesModelsCharacter} from './models-character'

describe('gamesModelsCharacter', () => {
  it('should work', () => {
    expect(gamesModelsCharacter()).toEqual('game-models-character')
  })
})
